# Copied to .env — loaded by Expo when you run from apps/mobile
# Required on a *physical* phone/tablet: use your machine's LAN IP, not localhost.
# Example: http://192.168.1.42:3000 (same Wi‑Fi as the device)
EXPO_PUBLIC_API_URL=http://localhost:3000
